# 本地落地与旧版保留

这是基于已核验旧工作流重新整合的独立候选包，不是对旧版本全部源文件的自动迁移。
技术名jiang-qiuzhi-skill，版本1.0.0-rc.1；建议与shengjiang-qiuzhao并存，验收前不要覆盖旧目录。

新包使用独立schema1数据库，与旧job-copilot后端不兼容。先用虚构资料验收，再导入用户确认的原简历、岗位表等文件；不得把旧SQLite文件复制过来直接运行，也不能声称保留了全部历史状态。

本次未复制原版后端、Kami/vendor、第三方字体、用户照片和任何运行数据库。文档规则和本地工具已经放在一个包里；联网、浏览器及PDF依赖仍需宿主实际具备并授权。

步骤：保留旧版→导入新包→用demo检查→在用户选择的独立工作区初始化→用户确认后处理真实资料。

中台原文的受控编辑和本包下载是两条独立路径。本次中台knowledge.write不足，未保存任何原文修改。
