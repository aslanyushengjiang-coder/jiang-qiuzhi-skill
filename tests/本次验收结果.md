# 本次验收结果｜2026-09-17

版本：1.0.0-rc.1。以下均是本候选包在独立测试环境的结果，不是用户电脑或真实招聘网站的验收。

## 实际通过

- 53项unittest全部通过，0失败、0跳过。覆盖数据隔离、事务回滚与备份、资料校验、URL去重、资格未知/不符、进度状态、时区、摘要隐私和简历证据引用。
- 临时生成的5000行虚构CSV全部导入，5000个唯一岗位；并不证明已经附赠或核实5000条真实岗位。
- 两页XLSX的CLI整链：导入13行、去重后12岗、1条重复；初筛输出12条及逐条原因。指定工作表导入、工作表不存在时失败也有测试。
- 离线虚构表单：5个字段填写与回读一致；选择本地演示PDF后核对文件名；声明未勾选、提交按钮禁用、无外部网络请求。
- 演示PDF：真实纸型A4竖版、1页、文字可提取；姓名文字使用Unicode NFKC标准化后核对（字体可能把部分字映射为兼容部首）；已渲染并目视检查，未见重叠、乱码或截断。没有进行真实招聘系统解析测试。
- 7页工作簿模板已导出；空模板看板5项均为0，公式错误扫描为0；已查看看板与演示岗位表渲染。

## PDF测试的环境限定

默认CLI保持Chromium沙箱开启。本环境以root运行，默认PDF命令被浏览器沙箱要求阻止；已正确输出失败状态并保留HTML/TXT，**默认CLI的PDF整链不记为通过**。

为单独核对排版与导出逻辑，只在一次性、断外网、虚构数据测试容器的测试程序中调用sandbox=False。未修改分发CLI默认值、用户机器设置或登录态。本次成功的PDF属于这一受控渲染验证，不能替代Mac/Windows实际安装后验收。

本机浏览器还限制file导航，因此分发导出器使用从本地读取的完整HTML字符串进行静态渲染，无需浏览器访问磁盘URL，也不加载远程资源。

## 尚未验证

新版在WorkBuddy的实际安装、自动触发、真实联网找岗、真实招聘网站填写/上传/跨站兼容性、完整六题面试会话及下一次会话复盘，均待实机验证。
基础工具处理的是结构化事实引用，不能自动证明每句改写语义完全真实，必须由用户核对。

## 不混淆旧版测试

旧版本地记录中的30项测试、官方岗位浏览、本地4字段代填及首题追问属于旧版的历史证据，不计入本包53项测试，也不替代新版验证。

## 查看与重跑

结构化结果：validation-summary.json。重跑：

```text
python -m unittest discover -s <SKILL_DIR>/tests -v
```

测试使用临时目录，测试结束清理；不接触真实网站和个人数据。Python及依赖不同时，结果须重新记录。
